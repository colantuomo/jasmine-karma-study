describe('Example test with node', () => {
    it('should show jasmine with node', () => {
        expect(true).toEqual(true);
    });

    it('should show karma running', () => {
        expect(1).toEqual(1);
    });
})
